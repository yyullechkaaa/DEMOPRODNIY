<Page x:Class="demoshoes.Pages.AuthPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:demoshoes.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource HeaderStyle}" Text="Авторизация" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Введите логин:" Style="{StaticResource TextStyle}"/>
            <TextBox x:Name="LoginBox" Style="{StaticResource TextBoxStyle}" Width="150"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Введите пароль:" Style="{StaticResource TextStyle}"/>
            <PasswordBox x:Name="PassBox" Width="150" HorizontalAlignment="Center" Height="30" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Войти" x:Name="SingInButton" Click="SingInButton_Click" Style="{StaticResource ButtonStyle}"/>
            <Button Content="Войти как гость" x:Name="SingInGuestButton" Click="SingInGuestButton_Click" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>
